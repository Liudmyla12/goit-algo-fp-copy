# goit-algo-fp

Фінальний проєкт до курсу **“Алгоритми та структури даних”**.

Репозиторій містить 7 незалежних завдань:

1. Однозв’язний список (reverse, sort, merge)
2. Фрактал “дерево Піфагора” (рекурсія)
3. Алгоритм Дейкстри з використанням бінарної купи (heapq)
4. Візуалізація бінарної купи (heap → tree)
5. Візуалізація обходів дерева DFS/BFS (стек/черга, без рекурсії)
6. Жадібний підхід vs динамічне програмування (вибір їжі)
7. Монте-Карло для двох кубиків + порівняння з аналітикою

---

## Структура репозиторію

```text
goit-algo-fp/
├── task_1.py
├── task_2.py
├── task_3.py
├── task_4.py
├── task_5.py
├── task_6.py
├── task_7.py
├── requirements.txt
├── .gitignore
├── pythagoras_tree.png
├── heap.png
├── dice_probabilities.png
└── traversal_output/
    ├── dfs_01.png ... dfs_06.png
    └── bfs_01.png ... bfs_06.png
```

````

---

## Встановлення та запуск

Рекомендовано використовувати **venv**.

### Windows PowerShell

```powershell
py -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install -r requirements.txt
```

### macOS / Linux

```bash
python3 -m venv .venv
source .venv/bin/activate
python -m pip install -r requirements.txt
```

---

## ✅ Task 1 — Однозв’язний список: reverse + sort + merge

**Файл:** `task_1.py`

Реалізовано:

- реверсування однозв’язного списку (перев’язування next-посилань);
- сортування (алгоритм для списку);
- злиття двох відсортованих списків в один відсортований.

Запуск:

```powershell
py .\task_1.py
```

---

## ✅ Task 2 — Фрактал “дерево Піфагора” (рекурсія)

**Файл:** `task_2.py`

Програма генерує фрактал і дозволяє задавати **рівень рекурсії**.

Запуск:

```powershell
py .\task_2.py --depth 10
```

Результат:

- зберігається зображення `pythagoras_tree.png`.

---

## ✅ Task 3 — Алгоритм Дейкстри (heapq)

**Файл:** `task_3.py`

Реалізовано пошук найкоротших шляхів у зваженому графі.
Для оптимізації вибору вершини використано **бінарну купу** (`heapq`).

Запуск:

```powershell
py .\task_3.py
```

---

## ✅ Task 4 — Візуалізація бінарної купи (heap → tree)

**Файл:** `task_4.py`

Побудовано дерево з масиву купи (індекси: `left=2i+1`, `right=2i+2`) та виконано візуалізацію через `networkx`.

Запуск:

```powershell
py .\task_4.py
```

Результат:

- зберігається зображення `heap.png`.

---

## ✅ Task 5 — Візуалізація обходів дерева DFS і BFS (без рекурсії)

**Файл:** `task_5.py`

Виконано:

- DFS (через стек)
- BFS (через чергу)

Кожен крок обходу зберігається як окремий кадр, вузли фарбуються від темних до світлих відтінків відповідно до порядку відвідування (RGB hex).

Запуск:

```powershell
py .\task_5.py
```

Результат:

- кадри зберігаються у `traversal_output/`:

  - `dfs_01.png ... dfs_06.png`
  - `bfs_01.png ... bfs_06.png`

---

## ✅ Task 6 — Жадібний алгоритм vs Динамічне програмування (Food selection)

**Файл:** `task_6.py`

- `greedy_algorithm`: вибір за максимальним співвідношенням `calories/cost`
- `dynamic_programming`: оптимальний набір за бюджетом (DP)

Запуск (приклад):

```powershell
py .\task_6.py --budget 100
```

---

## ✅ Task 7 — Монте-Карло: два кубики + порівняння з аналітикою

**Файл:** `task_7.py`

Виконується імітація великої кількості кидків двох кубиків:

- рахується ймовірність сум 2..12
- порівнюється з аналітичними значеннями
- будується графік

Запуск:

```powershell
py .\task_7.py --n 200000 --plot
```

Результат:

- зберігається графік `dice_probabilities.png`.

---

## Висновки

### ✅ Перевірка виконання
- Усі 7 завдань реалізовано.
- Кожен файл запускається окремо (`py .\task_X.py`) і відпрацьовує без помилок у середовищі з `requirements.txt`.
- Візуальні результати збережені у репозиторії як артефакти виконання.

### 🧩 Алгоритми та ключові ідеї
- **Task 1 (Linked List):**
  - Реверс списку зроблено через зміну посилань між вузлами (без створення нового списку).
  - Сортування виконано алгоритмом злиття (стабільно та ефективно).
  - Об’єднання двох відсортованих списків реалізовано коректно з перевіркою на тестових даних.
- **Task 3 (Dijkstra + heap):**
  - Реалізація з `heapq` дає ефективний вибір вершин (типово `O((V+E)\log V)`).
- **Task 6 (Greedy vs DP):**
  - Greedy — швидкий, але може не дати оптимум.
  - DP — повільніший, зате гарантує оптимальний набір страв у межах бюджету.

### 🎨 Візуалізація та артефакти
- **Task 2:** `pythagoras_tree.png` — фрактал “дерево Піфагора” з керованою глибиною рекурсії.
- **Task 4:** `heap.png` — візуалізація бінарної купи через побудову дерева.
- **Task 5:** `traversal_output/` — покрокові кадри DFS та BFS (без рекурсії, стек/черга).
- **Task 7:** `dice_probabilities.png` — графік імовірностей сум при киданні двох кубиків.

### 📊 Правильність результатів
- **Monte Carlo (Task 7):** отримані імовірності близькі до аналітичних значень; відхилення малі та очікувані для випадкової симуляції й зменшуються при збільшенні кількості кидків.

````
